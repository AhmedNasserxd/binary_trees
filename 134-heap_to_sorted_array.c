#include "binary_trees.h"

/**
 * heap_to_sorted_array - Transforms a MB Heap to a sorted array
 * @heap: a pointer to the node's root
 * @size: the address to use
 *
 * Return: the created array
 *         NULL on failure
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{

}
