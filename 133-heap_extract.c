#include "binary_trees.h"

/**
 * heap_extract - Obtains the node's root of a MB Heap
 * @root: a double pointer to use
 *
 * Return: the value stored in the node's root
 *         0 on failure
 */
int heap_extract(heap_t **root)
{

}
